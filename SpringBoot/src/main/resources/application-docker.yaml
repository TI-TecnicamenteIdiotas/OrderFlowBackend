server:
  port: 8080
  error:
    include-stacktrace: always
    include-message: always
  servlet:
    context-path: /nimbleflow-api

amazon:
  aws:
    accesskey: ${AWS_ACCESS_KEY}
    secretkey: ${AWS_SECRET_KEY}
  dynamodb:
    endpoint: ${NO_SQL_DATABASE_PATH}
    region: ${AWS_REGION}